sudo: required 
dist: trusty 
language: generic 
compiler:
  - gcc
env:
  matrix:
    - QUIET_CATKIN_COMMANDS=true USE_DEB=true  ROS_DISTRO="indigo" ROS_REPOSITORY_PATH=http://packages.ros.org/ros/ubuntu
    - QUIET_CATKIN_COMMANDS=true USE_DEB=true  ROS_DISTRO="indigo" ROS_REPOSITORY_PATH=http://packages.ros.org/ros-shadow-fixed/ubuntu
install:
  - git clone https://github.com/130s/industrial_ci.git -b suppress_msgs .ci_config  
script: 
  - source .ci_config/travis.sh
